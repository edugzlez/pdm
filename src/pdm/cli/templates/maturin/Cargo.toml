[package]
name = "example_package"
version = "0.1.0"
edition = "2021"

[lib]
name = "example_package"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "^0.22.2", features = ["extension-module"] }
